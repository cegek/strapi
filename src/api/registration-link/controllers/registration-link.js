'use strict';

/**
 * registration-link controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::registration-link.registration-link');
